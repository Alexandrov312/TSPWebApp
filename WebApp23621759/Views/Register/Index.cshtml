@model WebApp23621759.Models.ViewModel.Auth.RegisterUserViewModel

<div class="page-auth">
    <div class="card-auth">
        <h2 class="title">Register</h2>

        <form asp-action="Index" asp-controller="Register" method="post" class="auth-form">
            <div class="message-box">
                @if (ViewBag.ErrorMessage != null)
                {
                    <div class="error-message">@ViewBag.ErrorMessage</div>
                }
            </div>

            <div class="form-group">
                <label asp-for="Username">👤 Username</label>
                <input asp-for="Username" placeholder="Enter your username" />
                <div class="field-error">
                    <span asp-validation-for="Username"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Email">📧 Email</label>
                <input asp-for="Email" placeholder="Enter your email" />
                <div class="field-error">
                    <span asp-validation-for="Email"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="Password">🔒 Password</label>
                <div class="password-peek-field" data-password-peek>
                    <input asp-for="Password" type="password" placeholder="Enter your password" />
                    <button type="button" class="password-peek-btn" data-password-peek-toggle aria-label="Show password">⌣</button>
                </div>
                <div class="field-error">
                    <span asp-validation-for="Password"></span>
                </div>
            </div>

            <div class="form-group">
                <label asp-for="ConfirmPassword">🔒 Confirm password</label>
                <div class="password-peek-field" data-password-peek>
                    <input asp-for="ConfirmPassword" type="password" placeholder="Confirm your password" />
                    <button type="button" class="password-peek-btn" data-password-peek-toggle aria-label="Show password">⌣</button>
                </div>
                <div class="field-error">
                    <span asp-validation-for="ConfirmPassword"></span>
                </div>
            </div>

            <button type="submit" class="primary-btn">Register</button>

            <div class="signup-row">
                <span>Already have an account?</span>
                <a href="/Login">Login</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
