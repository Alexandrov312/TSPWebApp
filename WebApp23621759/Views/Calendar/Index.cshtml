@using WebApp23621759.Enums
@using WebApp23621759.Helpers
@model WebApp23621759.Models.ViewModel.Calendar.CalendarViewModel

@* Изчислява предишния и следващия месец за бутоните за навигация *@
@{
    var previousMonth = new DateTime(Model.Year, Model.Month, 1).AddMonths(-1);
    var nextMonth = new DateTime(Model.Year, Model.Month, 1).AddMonths(1);
}

<div class="page calendar-page">
    <div class="calendar-layout">
        <div class="calendar-panel">
            <div class="calendar-header">
                <a asp-action="Index"
                   asp-route-year="@previousMonth.Year"
                   asp-route-month="@previousMonth.Month"
                   class="calendar-nav-btn">←</a>

                <h2 class="calendar-title">@Model.MonthName</h2>

                <a asp-action="Index"
                   asp-route-year="@nextMonth.Year"
                   asp-route-month="@nextMonth.Month"
                   class="calendar-nav-btn">→</a>
            </div>

            <div class="calendar-weekdays">
                <div>Mon</div>
                <div>Tue</div>
                <div>Wed</div>
                <div>Thu</div>
                <div>Fri</div>
                <div>Sat</div>
                <div>Sun</div>
            </div>

            <div class="calendar-grid">
                @foreach (var day in Model.Days)
                {
                    <a asp-action="Index"
                       asp-route-year="@Model.Year"
                       asp-route-month="@Model.Month"
                       asp-route-selectedDate="@day.Date.ToString("yyyy-MM-dd")"
                       class="calendar-day @(day.IsCurrentMonth ? "" : "other-month") @(day.IsToday ? "today" : "") @(Model.SelectedDate.Date == day.Date.Date ? "selected" : "")">

                        <span class="day-number">@day.Date.Day</span>

                        @if (day.HasTasks)
                        {
                            <span class="task-dot"></span>
                        }
                    </a>
                }
            </div>
        </div>

        <div class="day-tasks-panel">
            <h3>Tasks for @Model.SelectedDate.ToString("dd.MM.yyyy")</h3>

            @* Зарежда списъка със задачи за избрания ден *@
            <partial name="_DayTasksList" model="Model" />
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/calendar.js" asp-append-version="true"></script>
    <script src="~/js/myTasks/shared.js" asp-append-version="true"></script>
    <script src="~/js/myTasks/subtask-panel.js" asp-append-version="true"></script>
    <script src="~/js/myTasks/task-actions.js" asp-append-version="true"></script>
    <script src="~/js/myTasks/subtask-actions.js" asp-append-version="true"></script>
}
